spring.application.name=manga-tracker-api

# Point Spring's autoconfigured DataSource at your SQLite file
spring.datasource.url=jdbc:sqlite:manga-tracker.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# (optional) be explicit about the port; 8080 is the default anyway
server.port=8080